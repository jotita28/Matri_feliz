<%# <% @services.each do |service| %>
      <div class="card col-sm-4 text-center p-3 border-light">
        <a href="<%= service_path(service) %>" data-turbolinks="false"><img src="<%= asset_path service.photo %>" class="card-img-top" alt="..."></a>
        <div class="card-body ">
        <h5 class="card-title text-info"><%= service.name %></h5>
        <p class="card-text"><%= service.detail%></p>
        <% if !current_user.favourites.pluck(:service_id).include? service.id %>
        <%=link_to 'Me encanta!', service_favourites_path(service), class: 'genric-btn primary circle', method: :post%>
        <% else %>
        <%=link_to 'Ya no me gusta', remove_service_path(service), class: 'genric-btn success circle', method: :delete%>
        <% end %>
        </div>
      </div>
    <%# <% end %>