<h1 class="text-center">Datos de <%= @service.name%></h1>
<div class="container pd-3">
  <table class="table">
    <thead class="table-dark">
      <tr>
        <th>Nombre</th>
        <th>Dirección</th>
        <th>Región</th>
        <th>Telefono</th>
        <th>Categoria</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @service.purveyor.name%></td>
        <td><%= @service.address%></td>
        <td><%= @service.region.name%></td>
        <td><%= @service.purveyor.phone%></td>
        <td><%= @service.todo.name%></td>
      </tr>
    </tbody>
  </table>
</div>